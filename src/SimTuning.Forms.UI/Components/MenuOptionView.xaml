<?xml version="1.0" encoding="UTF-8" ?>
<StackLayout x:Class="SimTuning.Forms.UI.Components.MenuOptionView"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:material="clr-namespace:XF.Material.Forms.UI;assembly=XF.Material"
             Padding="10"
             Orientation="Horizontal">
    <material:MaterialIcon x:Name="Icon"
                           Aspect="AspectFit"
                           HeightRequest="25"
                           Style="{StaticResource MaterialIconMediumStyle}"
                           WidthRequest="25" />

    <material:MaterialLabel x:Name="Label"
                            HorizontalOptions="StartAndExpand"
                            Style="{StaticResource MaterialLabelStyle}"
                            VerticalOptions="Center" />
</StackLayout>